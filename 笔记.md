## MuJoCo Deploy 关节顺序对齐问题笔记

### 1. 问题现象

在 `unitree_rl_gym/deploy/deploy_mujoco` 这条 MuJoCo 部署链里，`roboot16` 和 `roboot16_idealpd` 一开始都出现了类似现象：

- 左右腿发力明显不对称
- 看起来像“只有一边腿在动”
- 即使策略有迈步/站立意图，另一侧也像在乱发力或不配合

后面发现，这个问题并不主要是：

- `kps/kds` 太软
- 摩擦太小
- 某一侧 actuator 完全没收到控制

而是一个更基础的问题：

```text
训练时策略输出的动作顺序
和 deploy 里假设的关节顺序
不一致
```

---

### 2. 一开始 deploy 假设的顺序

最初在 deploy 里使用的是“按整条左腿、再整条右腿”的顺序：

```text
left_hip_pitch
left_hip_roll
left_hip_yaw
left_knee
left_ankle_pitch
left_ankle_roll
right_hip_pitch
right_hip_roll
right_hip_yaw
right_knee
right_ankle_pitch
right_ankle_roll
```

这个顺序和：

- MuJoCo XML 里的 actuator 排列
- 手工直觉里的“左腿 6 个 + 右腿 6 个”

是一致的。

但它**不是训练运行时策略输出的真实顺序**。

---

### 3. 如何确认训练真实顺序

后面专门写了脚本：

- `scripts/tools/inspect_roboot16_action_order.py`

去打印 IsaacLab 训练环境运行时的：

- `robot.joint_names`
- `joint_pos` 动作项最终解析出来的 `_joint_names`

打印结果为：

```text
00: left_hip_pitch_joint
01: right_hip_pitch_joint
02: left_hip_roll_joint
03: right_hip_roll_joint
04: left_hip_yaw_joint
05: right_hip_yaw_joint
06: left_knee_joint
07: right_knee_joint
08: left_ankle_pitch_joint
09: right_ankle_pitch_joint
10: left_ankle_roll_joint
11: right_ankle_roll_joint
```

也就是说，**训练时真正的动作顺序是“左右交替、按关节类型排列”**：

```text
L hip_pitch, R hip_pitch,
L hip_roll,  R hip_roll,
L hip_yaw,   R hip_yaw,
L knee,      R knee,
L ankle_pitch, R ankle_pitch,
L ankle_roll,  R ankle_roll
```

而不是 deploy 里原来假设的：

```text
左腿 6 个 + 右腿 6 个
```

---


### 4. 最终修正方案

后面把以下 deploy 配置都改成了**训练真实顺序**：

#### roboot16

- `unitree_rl_gym/deploy/deploy_mujoco/configs/roboot16.yaml`

#### roboot16_idealpd

- `unitree_rl_gym/deploy/deploy_mujoco/configs/roboot16_idealpd.yaml`
也就是说，现在 deploy 使用的顺序已经变成：

```text
left_hip_pitch_joint
right_hip_pitch_joint
left_hip_roll_joint
right_hip_roll_joint
left_hip_yaw_joint
right_hip_yaw_joint
left_knee_joint
right_knee_joint
left_ankle_pitch_joint
right_ankle_pitch_joint
left_ankle_roll_joint
right_ankle_roll_joint
```

---



### 5. 当前经验结论
在进行 MuJoCo 部署时，**必须确保 deploy 里假设的动作顺序和训练时策略输出的真实顺序完全一致**，deploy中的顺序一般在yaml配置里显式制定，但宇树gym中并没有指定，因为可能关节默认顺序跟其加载mujoco的脚本的关节顺序是匹配的，但我们的自己的是不匹配的。

## MuJoCo Deploy 观测&控制器对齐笔记
### 1. 观测对齐问题
主要观测注意喂入策略网络的顺序以及其对应的坐标系定义，确保与训练时一致。
- 在issaclab中观测的base的线速度角速度都是base自己坐标系下的；
- 但在deploy_mujoco里，通过data.qvel获取的base线速度(0:3)是世界坐标系下的，需要转换到base坐标系下才能喂入策略网络;而角速度(3:6)又直接是base坐标系下的，不需要转换。
在我的代码中坐标系是对齐了的，但是可以作为一个纠错的关键点。
### 2. 控制器对齐问题
1. 在isaaclab训练时，我给机器人模型的关节配置的ImplicitActuator，相关的参数为stiffness和damping，核心控制过程是**给底层物理仿真器输出目标的关节位置**，底层自动去动力学解析输出力矩；
2. 我仿照的unitree_rl_gym中deploy_mujoco脚本中关于控制器使用的是pd控制，输出的是力矩（这实际是因为gym中训练的控制器采取的是IdealPDActuator,这个输出的刚好也是力矩，计算方式跟pd控制一致）

3. 我一开始deploy给自己的模型关节设置的是pd控制，显然是没有对齐我训练时候的，后面更改了电机的类型改为了position，这样就和训练时的控制方式对齐了。其实之前也改过一版，但是当时的关节顺序是错的，所以觉得改了没有什么效果。

### 3.解决方法
1. mujoco中使用position的控制器来对齐isaaclab的ImplicitActuator;
低速的策略网络成功实现了部署，可控速度范围在0-1.2m/s。
2. 目前高速的策略网络部署失败，怀疑是步态问题导致的碰撞，后续使用amp去训练更好的步态。
3. 学长指出**提高环境的仿真频率**后，position控制器会跟impliciactuator更对齐。
re:尝试了，响应似乎更符合isaacsim训练时的，但策略的步态问题还是主要原因，还是要去训练更好的步态。

### AMP训练待办
1. 先调整一下奖励权重，在没有amp奖励的参与下跟踪速度
2. 裁切合适的重定向的数据，向前跑的；
3. 分析一下为什么style reward的数量级会很大；

## AMP 重定向数据迁移笔记
主要参考天工的amp配置，基于amp-rsl-rl强化学习库进行修改，得到manager-based的AMP训练框架；
主要流程如下：
- 先基于GMR库进行重定向，得到pkl数据（基于MuJoCo）；
- 将pkl数据转换为 AMP-expert 的txt数据（基于MuJoCo）；
- 在 isaacsim 中对于单独的txt/txt+pkl进行回放，可视化验证关节顺序的对齐；
- 最终基于 isaaclab 中进行 AMP 训练，验证整体流程的正确性(已验证)；

在这套流程中需要重点关注下面几个问题：
1. Mujoco中重定向得到的pkl中数据对应的关节顺序
2. isaacsim 实际 AMP 观测内容，是否和 `txt` 里的 AMP 数据内容对齐
3. isaacsim 实际 AMP 观测顺序，是否和 MuJoCo 导出的 AMP 数据（txt/pkl）顺序对齐

---

### 1. Mujoco导出pkl的关节顺序
首先需要注意的是，GMR自带脚本导出的pkl是不会包含关节顺序的，无论是转换为txt，还是转换为npy给amp-rsl-rl标准库使用，都需要手动加入这个关节顺序，告诉数据加载器数据集每一维对应的关节是什么。

这个关节顺序从mujoco确定，pkl是直接保存了每一帧的qpos和qvel，具体如下：
- root_pos：3 # 世界坐标系下 root 位置
- root_rot：4 # 世界坐标系下 root 四元数
- dof_pos：12 # 按照 MuJoCo 模型里定义的顺序，保存了 12 个关节的绝对位置（如果没有手动设置ref就是绝对位置，12维为我的机器人模型的关节数）
- dof_vel：12 # 按照 MuJoCo 模型里定义的顺序，保存了 12 个关节的绝对速度

这个关节顺序一般是跟xml资产里定义的顺序一致的，我自己的先是右腿6个关节从上到下，再是左腿6个关节。
不确定也可以去在viewer时打印一下对应的关节顺序（写一个脚本）
```python
JointOrder.right_leg_q = [
    "right_hip_pitch_joint",
    "right_hip_roll_joint",
    "right_hip_yaw_joint",
    "right_knee_joint",
    "right_ankle_pitch_joint",
    "right_ankle_roll_joint",
]
JointOrder.left_leg_q = [
    "left_hip_pitch_joint",
    "left_hip_roll_joint",
    "left_hip_yaw_joint",
    "left_knee_joint",
    "left_ankle_pitch_joint",
    "left_ankle_roll_joint",
]
```

然后将这个pkl转换为txt，脚本将pkl在mujoco中重放，获取脚部link在root局部坐标系下的位置，保存为txt时，数据部分按照下面的顺序保存：
```text
right_leg_q(6),
left_leg_q(6),
right_leg_dq(6),
left_leg_dq(6),
left_foot_pos_local(3),
right_foot_pos_local(3)
```
同时在txt文件的header里列出了关节顺序表，即前面的JointOrder.right_leg_q等内容，方便后续loader里对齐。

motion_loader.py 读取位置：
```python
right_leg_names = list(joint_order.get("right_leg_q", []))
left_leg_names = list(joint_order.get("left_leg_q", []))
dataset_joint_names = right_leg_names + left_leg_names
```
---

### 2. amp数据集与isaacsim内容含义对齐
最终会用来训练的是上面导出的txt文件，主要包括关节位置、关节速度、足端位置等内容；
- 关节位置在mujoco没有额外设置ref的情况下是绝对位置；
- 关节速度是绝对速度（相对0的速度）；
- 足端位置是root局部坐标系下的位置；

在isaacsim的amp观测内容需要跟数据集的语义对齐，实际使用的是：
- joint_pos -> 绝对关节位置
- joint_vel -> 绝对关节速度
- foot_pos -> root局部坐标系下的足端位置（isaaclab中没有可直接调用的观测，需自己实现）

---

### 3. amp数据集与isaacsim观测顺序对齐
amp数据集的关节顺序前面已经说的很清楚了，是mojuco中的顺序。但是在isaacsim实际训练中，观测的关节顺序是不一样的，写了一个脚本打印了实际观测的关节顺序qpos[]，发现是下面这个顺序：
```python
AMP_JOINT_NAMES = [
    "left_hip_pitch_joint",
    "right_hip_pitch_joint",
    "left_hip_roll_joint",
    "right_hip_roll_joint",
    "left_hip_yaw_joint",
    "right_hip_yaw_joint",
    "left_knee_joint",
    "right_knee_joint",
    "left_ankle_pitch_joint",
    "right_ankle_pitch_joint",
    "left_ankle_roll_joint",
    "right_ankle_roll_joint",
]
```
所以在实际amp算法中需要将数据集和实际观测顺序对齐，才能保证有效性，实现主线程如下：
- Isaac Sim 按 `AMP_JOINT_NAMES` 产生真实 AMP 观测
- MuJoCo `txt` 在 loader 中按名字映射到 `AMP_JOINT_NAMES`

```python
idx_map: List[Union[int, None]] = []
for joint_name in expected_joint_names:
    if joint_name in dataset_joint_names:
        idx_map.append(dataset_joint_names.index(joint_name))
    else:
        idx_map.append(None)
```
这里的 `expected_joint_names` 就是 runner 传进来的环境顺序，也就是 `AMP_JOINT_NAMES`。

经过上述操作就将txt中的数据正确映射到了isaacsim的观测顺序，保证了训练的有效性。
